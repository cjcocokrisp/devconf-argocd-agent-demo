apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emulatorjs-data
  labels:
    app: emulatorjs
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
