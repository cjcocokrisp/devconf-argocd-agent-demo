apiVersion: v1
kind: Service
metadata:
  name: nginx-emulatorjs-service
  labels:
    apps: nginx-emulatorjs
spec:
  selector:
    app: nginx-emulatorjs
  type: LoadBalancer
  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 443
